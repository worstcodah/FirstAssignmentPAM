<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.assignment1pam">

    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Assignment1PAM">
        <activity android:name=".activities.FourthActivity"></activity>
        <activity android:name=".activities.ThirdActivity" />
        <activity android:name=".activities.SecondActivity" />
        <activity
            android:name=".activities.FirstActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Assignment1PAM.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>